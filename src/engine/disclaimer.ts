/**
 * Protected legal copy (CLAUDE.md §6). This string is verbatim and must appear
 * on every report surface — web and CLI. Never edit, shorten, or relocate it.
 * A test asserts this exact text; changing it fails the suite by design.
 */
export const LEGAL_DISCLAIMER: string =
  "Preflight is an educational tool. It shows publicly documented statistics " +
  "about market situations. It does not know your finances, does not give " +
  "investment advice, and does not recommend any transaction. Nothing here is " +
  "a solicitation to buy or sell any security. Trading involves substantial " +
  "risk of loss.";

/** Rendered verbatim wherever a field could not be computed (CLAUDE.md §2.1). */
export const DATA_UNAVAILABLE: string = "Data unavailable";
